# Line to Note

Статус: исходники текущей версии ещё не перенесены в общий репозиторий.

Папка зарезервирована под плагин переноса строки в указанную заметку. До появления реальных `main.js` и `manifest.json` плагин не включён в `registry.json`.
