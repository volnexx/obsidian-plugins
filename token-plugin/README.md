# Token Plugin

Статус: исходники текущей версии ещё не перенесены в общий репозиторий.

Папка зарезервирована под наш плагин подсчёта токенов. До появления реальных `main.js` и `manifest.json` плагин не включён в `registry.json`.
