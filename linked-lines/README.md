# Linked Lines

Статус: исходники текущей версии ещё не перенесены в общий репозиторий.

Папка зарезервирована под плагин синхронизации строк. До появления реальных `main.js` и `manifest.json` плагин не включён в `registry.json`.
