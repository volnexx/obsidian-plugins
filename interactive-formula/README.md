# Interactive Formula

Статус: исходники текущей версии ещё не перенесены в общий репозиторий.

Папка зарезервирована под актуальный `main.js`, `manifest.json` и `styles.css`. До появления реальных файлов плагин не включён в `registry.json`.
